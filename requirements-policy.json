{
  "files": {
    "requirements.txt": {
      "allow_unpinned": {
        "numpy": "Bounded to numpy 1.x for compatibility with torch/OpenCV and the local research stack."
      }
    },
    "requirements-dev.txt": {
      "allow_unpinned": {
        "numpy": "Bounded to numpy 1.x for local tests and WSL research environments."
      }
    },
    "requirements-libero.txt": {
      "allow_unpinned": {}
    }
  }
}

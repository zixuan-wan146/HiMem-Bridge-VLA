# 2026-06-23 工作记录

## 当前口径

- 不再使用版本号命名；当前方案直接叫 Dual-FIFO Visual Token Memory。
- 先处理 memory 侧推理路径，不先接 Bridge-Attention。
- KFS 暂时不写；长期 memory 先支持 external/oracle keyframe 写入。
- memory entry 不包含 `metadata_i`，只包含 visual tokens、$\tau_i$、$\eta_i$。
- compression 默认接收已经对齐到模型 hidden dim 的 visual tokens，不额外引入 per-view projection。
- LIBERO / 双视角单臂默认 `n_m=1`；双臂 / 三摄像机默认 `n_m=2`。
- 当前不推进训练相关接口。
- 当前不做真实 checkpoint + HiMem server 的 RMBench 端到端 eval smoke。
- 当前不做 LIBERO-Plus robustness eval wrapper。

## 今日要做

1. [x] 更新中文设计文档，去掉版本号叙事，明确推理优先实现。
2. [x] 更新 Q&A，解释 projection、type embedding、训练方式和 KFS 暂缓原因。
3. [x] 实现 memory 侧推理组件：
   - `VisualMemoryEntry`
   - deterministic short FIFO
   - external/oracle long FIFO
   - padding + memory mask
   - `VisualMemoryCompressor`
4. [x] 增加单测：
   - short memory 按 low-level offset 读取
   - long FIFO 容量和顺序
   - padding mask 扩展到 token mask
   - compressor 输出 shape
   - padding token 不泄漏为可学习 null token
5. [x] 删除旧 HiMem/retrieval/controller 路径，避免和当前 Dual-FIFO memory 并列存在。
6. [x] 调研 LIBERO、LIBERO-Plus、RMBench 的本机数据状态和官方入口。
7. [x] 增加 benchmark inventory 脚本，记录本机 benchmark 资产。
8. [x] 实现 RMBench HDF5 episode reader，作为后续 memory replay / normalization / eval adapter 的输入基础。
9. [x] 实现 RMBench normalization stats builder，输出与现有 normalizer 兼容的 `norm_stats.json`。
10. [x] 实现通用 memory replay index 规则和 RMBench JSONL index builder。
11. [x] 实现 LIBERO JSONL memory replay index builder。
12. [x] 实现 LIBERO / RMBench replay frame reader，能从 index 回读图像、state 和 future action chunk。
13. [x] 实现 memory replay PyTorch dataset / collate，作为后续 visual token cache 或训练 adapter 的输入层。
14. [x] 实现 visual token replay cache builder，把 replay frame 样本编码成 shard + manifest。
15. [x] 在远端运行 token cache smoke / 单测。
16. [x] 实现 visual token cache dataset / collate，能从 shard 回读训练 batch 并构造 short `MemoryReadResult`。
17. [x] 实现 memory 侧训练 smoke adapter，验证 token cache dataset、short memory compression、action loss、checkpoint/snapshot 链路。
18. [x] 实现 RMBench eval command planner，生成官方 direct/socket eval 命令并检查任务、policy config 和数据目录。
19. [x] 实现 RMBench official policy adapter，把 RMBench observation 转成 HiMem server request，并把 action chunk 回放到 `TASK_ENV.take_action`。
20. [x] 实现 RMBench policy adapter 安装脚本，能复制到官方 `policy/HiMemBridgeVLA/`。
21. [x] 实现 RMBench eval run wrapper 和 manifest writer，让安装 adapter、写 plan、逐任务官方 eval 有固定入口。
22. [x] 同步远端和本地文件内容。

## 当前推理组件状态

- 已写：Dual-FIFO visual memory、padding mask、view-aware learned-query compression。
- 已写：RMBench 轻量 episode reader，用于读取多相机图像、14 维 action、双臂 endpose/gripper 和 instruction。
- 已写：RMBench normalization stats builder，用于训练前固定 state/action min-max 统计。
- 已写：RMBench memory replay index builder，用于固定当前帧、short memory 历史帧和 action chunk 范围。
- 已写：LIBERO memory replay index builder，用于固定当前帧、short memory 历史帧和 action chunk 范围。
- 已写：LIBERO / RMBench replay frame reader，用于从 index 回读当前/短期历史图像、state 和 future actions。
- 已写：memory replay PyTorch dataset / collate，用于把 replay frame 样本变成训练入口可消费的 batch。
- 已写：visual token replay cache builder，用于把 replay frame 图像预编码成按 view 分组的 visual tokens。`image_stats` 只用于测试和 smoke；真实训练缓存使用 InternVL3 visual tower。
- 已写：visual token cache dataset / collate，用于从 shard 回读 current tokens、short tokens、state、future actions、`short_steps` 和 `short_mask`，并为每个样本构造 short `MemoryReadResult`。
- 已写：token-cache batch 到 `memory_context` / `memory_context_mask` 的共享构造入口，并已让 Bridge-Attention 接受 `memory_context_mask`。
- 已写：memory 侧小型动作回归 smoke adapter，作为历史链路检查工具保留；当前不运行、不扩展。
- 已写：RMBench eval command planner，用于生成官方 `script/eval_policy.py` direct 模式和 `policy_model_server.py` + `eval_policy_client.py` socket 模式命令。
- 已写：RMBench official policy adapter，源码在 `evaluations/rmbench/policy/HiMemBridgeVLA/`，安装脚本为 `scripts/install_rmbench_policy_adapter.py`。
- 已写：RMBench eval run wrapper：`scripts/run_rmbench_eval.sh`，以及 run manifest writer：`scripts/write_rmbench_run_manifest.py`。
- 未写：KFS，按当前决定暂缓。
- 未写：Bridge-Attention 消费 memory context，按当前决定暂缓。
- 不推进：正式 Bridge/action-head 训练 loop、token cache / memory context 训练接入。
- 不推进：真实 checkpoint + HiMem server 的 RMBench 端到端 eval smoke。
- 不推进：LIBERO-Plus robustness eval wrapper。

## 暂不做

- 不写 KFS。
- 不接 Bridge-Attention。
- 不改 action head。
- 不新增训练相关接口。
- 不做全量训练入口。
- 不做真实 checkpoint/server eval smoke。
- 不做 LIBERO-Plus robustness eval wrapper。
- 不引入额外版本号配置名。

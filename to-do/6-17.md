# 2026-06-17 To-Do

## Current Judgment

The 35-step LIBERO trace smoke is an observability check, not a performance result.
It is reasonable that no transition trigger fired in this short rollout:

```text
trace rows: 35
ready/scored rows: 3
score range: 0.5269 to 0.5480
soft_plan / hard_plan / memory_write: 0 / 0 / 0
```

The selected runtime thresholds are:

```text
planner_threshold = 0.70
memory_write_threshold = 0.80
```

Since the highest observed score was below `0.70`, no replan or memory write should be expected.

Project organization entry points:

```text
docs/current_project_state.md
to-do/project_cleanup_plan.md
docs/project_structure.md
```

## Completed

- Transition trigger training, ablation, and offline replay records are available.
- Selected runtime package is available on the remote server.
- InternVL3-1B cache is present under `/root/autodl-tmp/hf-home`.
- HiMem server can start with the transition trigger package.
- LIBERO client can send `transition_frame` and receive action chunks.
- Per-decision LIBERO transition JSONL trace logging has been added.
- One 35-step trace smoke generated a valid 35-line JSONL trace.
- Trace summary utility has been added and verified:

```bash
python scripts/summarize_libero_transition_trace.py \
  runs/transition_trigger/libero_closed_loop_trace_smoke/20260617_212202_trace_jsonl_smoke/client/results/transition_trace_smoke_transition_trace.jsonl \
  --format text
```

Latest smoke summary:

```text
trace_rows: 35
ready_rows: 3
scored_rows: 3
score_min / score_mean / score_max: 0.5269 / 0.5386 / 0.5480
soft_plan_count: 0
hard_plan_count: 0
memory_write_count: 0
should_plan_count: 0
chunk_shortened_count: 0
error_rows: 0
```
- Longer trace eval has been run on the remote server:

```text
run: /root/autodl-tmp/HiMem-Bridge-VLA/runs/transition_trigger/libero_closed_loop_trace_eval/20260617_214140_long_trace_eval
task_suite: libero_spatial
task_limit: 3
episodes: 1
max_steps: 80
client_status: 0
result: 0/3 success, all three episodes ended with max_steps_exhausted
```

Trace summary:

```text
trace_rows: 240
ready_rows: 144
scored_rows: 144
score_min / score_mean / score_max: 0.1409 / 0.5473 / 0.6934
soft_plan_count: 0
hard_plan_count: 0
memory_write_count: 0
should_plan_count: 0
chunk_shortened_count: 0
error_rows: 0
```

Interpretation:

```text
The closed-loop integration is stable across three LIBERO spatial tasks.
The trigger did not fire because the maximum observed score, 0.6934, remained just below
planner_threshold = 0.70. This is a calibration/threshold question, not a transport or logging bug.
```

## Remaining Work

### 1. Clean Up Commits

Split the current dirty tree into readable commits:

```text
1. transition_trigger runtime and selected config
2. HiMem server + LIBERO transition-frame integration
3. LIBERO JSONL trace logging and manifest support
4. experiment documentation
```

### 2. Decide Runtime Policy For LIBERO Demos

The selected offline policy uses:

```text
planner_threshold = 0.70
memory_write_threshold = 0.80
```

The longer LIBERO trace peaked at `0.6934`, so the current policy is conservative in closed loop.
Before making LIBERO-facing claims, choose one of these:

```text
1. Keep 0.70 and describe LIBERO as integration/observability only.
2. Add an explicit demo/calibration threshold such as 0.65 for LIBERO trace visualization.
3. Calibrate or train on LIBERO-like closed-loop traces before claiming LIBERO behavior.
```

### 3. Decide LIBERO Claim Scope

For now, LIBERO should be described as integration/observability smoke unless we train or calibrate
for LIBERO. The offline transition-trigger result is the actual experimental evidence.

### 4. Paper Figure

Use one small temporal-window ablation figure:

```text
metric: Transition-F1 (%)
W24: 46.5
W32: 47.9
W48: 36.8
```

Message:

```text
Medium temporal context is best; longer history is not automatically better.
```

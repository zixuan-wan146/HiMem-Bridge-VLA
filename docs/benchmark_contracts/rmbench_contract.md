# RMBench Benchmark Contract

RMBench protocol code belongs in `himem_bridge_vla.benchmarks.rmbench`. RMBench view, state, action, normalization, and official policy-adapter rules must stay out of model and runtime modules.

The RMBench adapter is responsible for request construction, action decoding, and compatibility with official evaluation entry points.
